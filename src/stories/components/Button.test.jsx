import React from 'react';
import { expect } from '@storybook/jest';

test('sample test that checks for true', () => {
  // eslint-disable-next-line jsx-a11y/anchor-has-content
  
  expect(1).toBe(1);
});
